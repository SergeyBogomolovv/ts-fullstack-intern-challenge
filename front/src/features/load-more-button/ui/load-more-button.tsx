import styled from "styled-components";

export default function LoadMoreButton() {
  return <Button>... загружаем еще котиков ...</Button>;
}

const Button = styled.button`
  background: none;
  border: none;
  cursor: pointer;
  font-size: 16px;
`;
